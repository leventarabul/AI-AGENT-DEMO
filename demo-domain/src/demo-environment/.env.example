# Database Configuration
# Change these values in your local .env file
DB_HOST=localhost
DB_PORT=5432
DB_NAME=campaign_demo
DB_USER=admin
DB_PASSWORD=

# API Server Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Job Processor Configuration
POLL_INTERVAL=5

# API Basic Authentication (stored in database configuration table)
# Set these values in your local .env file
# DO NOT commit credentials to git
API_USERNAME=
API_PASSWORD=


